/**
 * Provides the classes and methods responsible for managing the various operations 
 * in the Hospital Management System (HMS). 
 * 
 * <p>
 * The controller package acts as the intermediary 
 * between the model (data) and the view, processing user inputs and updating the system state accordingly.
 * </p>
 * 
 * @since 1.0
 */
package src2.controller;
